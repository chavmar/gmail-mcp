{
  "name": "gmail-mcp-remote",
  "version": "0.1.0",
  "description": "Remote HTTP wrapper for the Gmail MCP stdio server",
  "type": "module",
  "scripts": {
    "start:remote": "node remote/gmail-remote.js",
    "check:remote": "node --check remote/gmail-remote.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0"
  }
}
